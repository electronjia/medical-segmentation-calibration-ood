
March 8 2026
- matplotlib can not plot ct images in subplots and save them. instead used plt.imsave to save individual CT images.

march 10, 2026
tensorflow vs pytorch data loading
- tensorflow accepts data in format (batch, x,y,z,c) where x=width, y=height, z=depth, c=channels. 
- pytorch accepts the data in format (batch, channels, z=depth, y=height, x=width).

loss funciton may require ne-hot encoding
- in pre_processing.py file, line ~223 includes Y_vol definition without n_class in (n_class, z,y,x) dimension for one hot encoding
- make sure to change the above if the loss function requires one hot encoding

ran into an array mismatch error: "
    Traceback (most recent call last):
  File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 333, in <module>
    pre_processor.process_all_volumes()
  File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 270, in process_all_volumes
    Y_vol[index, indiv_class, ...] = (final_label == indiv_class).astype(np.float32)
    ~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^
    ValueError: could not broadcast input array from shape (160,159,159) into shape (160,160,160)"

- to do: ensure the cropped segmentation overlaps properly the input. compare original segmentation+input overlap and processed segmentation+input overlap


March 12, 2026
- SITK order of index and dimensions explanation (x,y,z): https://simpleitk.org/SimpleITK-Notebooks/01_Image_Basics.html
- resolution to above dimension mismatch issue originated from mismatching input and label volume spacing. in the resampling, i used the image grid produced by the input volume and re-used this image grid on the label volume while resampling. this seemed to have resolved the issue.
- i was also running into a lot of issues using matplotlib pyplot imshow function. it just did not work on anything. i used cv2 to create overlay masks for debugging.
- i load the CT scan using SITK which gives xyz, then get numpy array which gives zyx, then transpose to xyz

- ran into an error with hdf5 files:   File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 408, in <module>
      File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 400, in process_all_volumes

      File "C:\Users\mkkad\anaconda3\Lib\site-packages\h5py\_hl\group.py", line 183, in create_dataset
        dsid = dataset.make_new_dset(group, shape, dtype, data, name, **kwds)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
      File "C:\Users\mkkad\anaconda3\Lib\site-packages\h5py\_hl\dataset.py", line 104, in make_new_dset
        dcpl = filters.fill_dcpl(
              ^^^^^^^^^^^^^^^^^^
      File "C:\Users\mkkad\anaconda3\Lib\site-packages\h5py\_hl\filters.py", line 159, in fill_dcpl
        raise TypeError(
      TypeError: Scalar datasets don't support chunk/filter options


March 14, 2026
- the solution to above issue was two-fold: 1. convert all data types to eligible data types using convert_for_json function. 2. use json data format and not dictionary to write it to hdf5 file. I opened the saved hdf5 and the following were the shapes: X=[N,1,ZYX]
- ran into another error with matching the images after resampling and cropping to obtain overlay figures for debugging. changed the code again.
- cropping of kits is definitely wrong or it seems wrong? because the ct images are cropped by quite a lot: read the code and it seems like it needs to be cropped to make sure only rlevant stuff is passed to model, otherwise the data is huge

Match 15, 2026
- ran into an error with kits19 data saving into hdf5 and it was due to me missing to reset the index when subsetting the dataframe.
- the task 2 heart overlay images look inconsistent. i dont know what to do about this so i leave it be. the task 3 liver overlay figures look good tho
- i changed the dimensions of non-cube dimensions to 160,160,160 for consistency. i do not know if that was a good step tho.

March 16, 2026
- i ran into this error with task 3 liver data (not used in the paper): Processing Task03_Liver: 0%| | 0/107 [00:23<?, ?it/s] 
  Traceback (most recent call last): 
  File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 442, in <module> 
  pre_processor.process_all_volumes() File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 329, in process_all_volumes 
  resampled_seg_extent = self.get_segmentation_extent(resampled_label_array) # get segmentation extent of the resampled label volume 
  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ 
  File "C:\Users\mkkad\Desktop\github_repos\medical-segmentation-calibration-ood\pre_processing.py", line 43, in get_segmentation_extent 
  non_zero_label[0].min(), non_zero_label[0].max(), 
  ^^^^^^^^^^^^^^^^^^^^^^^ 
  File "C:\Users\mkkad\anaconda3\Lib\site-packages\numpy\core\_methods.py", line 45, in _amin 
  return umr_minimum(a, axis, None, out, keepdims, initial, where) ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ ValueError: zero-size array to reduction operation minimum which has no identity

- reason why above error was happening: the label classes of original label were 3 classes (e.g. 0,1,2) but the label classes of resampled label was just 1 (e.g 0). GPT mentioned that it might be due to resampling funciton. in the function label array does not have the properties of input image assigned to it (e.g. spacing, origin, direction). I changed the code to include these properties when resampling the label. it seemed to have fixed the code
- need to confirm the other datasets are still functional with above change. the task liver still had issues due to label array mismatch.


Mar 24, 2026
- i verified today that the chunking piece of code des work properly. i tested it out with task heart data (index 2). Initially the data had size of (6,1,160,160,160) and after chunking it to 96^3 with stride=24 giving 3 patches (3^3=27 gives that much patches for the original volume). Heart dataset has 6 unique volumes with original 160^3 dimension and produces 162 patches after chunking.
- ideas for improving train: generate n_train_samples while doing center jitter. len becomes = len(x) * len(n_train_samples) and in the get_item, introduce a for loop
- validation and test evaluation, i can use monai for chunking data, predicting, and stitching chunks back
- make sure the loss function accepts the format of predictions and labels which is batch number, zyx